import React from 'react';
import styled from "styled-components";

const LoaderChildWrapper = styled.div``

const LoaderChild: React.FC = () => {
    return (
        <LoaderChildWrapper></LoaderChildWrapper>
    );
};

export default LoaderChild;